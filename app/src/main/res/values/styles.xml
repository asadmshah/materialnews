<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Toolbar">
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="theme">@style/Toolbar.Theme</item>
        <item name="titleTextAppearance">@style/Toolbar.TitleTextAppearance</item>
        <item name="subtitleTextAppearance">@style/Toolbar.SubTitleTextAppearance</item>
    </style>

    <style name="Toolbar.Theme">
        <!--<item name="android:textColorPrimary">@color/white_primary_text</item>-->
        <!--<item name="android:textColorSecondary">@color/white_secondary_text</item>-->
    </style>

    <style name="Toolbar.TitleTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <!--<item name="android:textSize">16sp</item>-->
        <item name="android:textColor">@color/white_primary_text</item>
        <!--<item name="android:textStyle">bold</item>-->
    </style>

    <!-- Includes back button and three dots -->
    <style name="Toolbar.SubTitleTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Subtitle">
        <!--<item name="android:textSize">14sp</item>-->
        <item name="android:textColor">@color/white_secondary_text</item>
        <!--<item name="android:textStyle">italic</item>-->
    </style>

</resources>
